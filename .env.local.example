# Local development environment variables
# Copy this to .env.local and fill in actual values

# Database connection (via SSH tunnel)
# Run: ssh -L 15432:magenta-postgres:5432 root@hunter.cryptograss.live
POSTGRES_HOST=host.docker.internal
POSTGRES_PORT=15432
POSTGRES_DB=magenta_memory
POSTGRES_USER=magent
POSTGRES_PASSWORD=<get-from-hunter>

# Django settings
DJANGO_SECRET_KEY=<your-local-secret-key>
